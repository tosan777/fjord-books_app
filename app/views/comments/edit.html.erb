<h1>コメント編集ページ</h1>
<%= form_with model: @comment, url: polymorphic_path([@commentable, @comment]), method: :patch do |f| %>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </div>

  <div class="action">
    <%= f.submit '編集する' %>
  </div>

<% end %>
