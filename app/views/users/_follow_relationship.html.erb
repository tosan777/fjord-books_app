<% if users.exists? %>
  <% users.each do |user| %>
    <div class="one-post card w-100">
      <%= link_to user_path(user) do %>
        <p class="caption h4"><%= user.name %></p>
      <% end %>
    </div>
  <% end %>
<% else %>
  <h3>ユーザーはいません</h3>
<% end %>
